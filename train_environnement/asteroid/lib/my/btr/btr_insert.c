/*
** EPITECH PROJECT, 2019
** my_compute_power_it
** File description:
** hello
*/

#include "../my.h"
#include <stdlib.h>

void btr_insert(btr_t *btr, void *data)
{
    int (*cmp)(void *) = btr->cmp;
    brs_t *brs = btr->brs;
    while (brs){
        for (int i = 0; cmp(1) < 0; i++);
    }

}
